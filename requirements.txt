numpy==2.4.6
scipy==1.17.1
rasterio==1.4.4
segmentation-models-pytorch==0.5.0
topotoolbox==0.0.12
torch==2.11.0
torchvision==0.26.0



PyTorch GPU support is optional.
For NVIDIA CUDA acceleration, install the appropriate PyTorch build
for your system from the official PyTorch installation instructions.
The plugin falls back to CPU when CUDA is unavailable.

Nvidia GPU acceleration is available: pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128

CPU-only installation is available: pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cpu